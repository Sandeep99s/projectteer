// Set the gift code for today
window.siteConfig.giftCode.code = "A54JB984SLNO0";

// Add valid UIDs if needed
window.siteConfig.giftCode.validUids = [/* your UIDs here */];

// Log when this version was loaded (with timestamp)
console.log("Gift code loaded at: " + new Date().toString());